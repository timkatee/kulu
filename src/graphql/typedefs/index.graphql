scalar Date
scalar JSON
#
enum CrudOperations{
    CREATE,
    UPDATE,
    DELETE
}
#
interface SchemaDefaults{
    created_at:Date,
    updated_at:Date,
    status:Boolean
}
#root query
type Query{
    # node metadata
    NodeMetaDataDb(nodeName:String!,seqQueryOptions:JSON):NodeMetaDataDb
    # users
    User(id:Int):User
    Users(seqQueryOptions:JSON):[User]
}

#root mutation
type Mutation{
    # user
    createEditUser(user:UserInput!,type:CrudOperations):User!
    # organization
}